import { ThemeBackgroundPipe } from './theme-background.pipe';

describe('ThemeBackgroundPipe', () => {
	it('create an instance', () => {
		const pipe = new ThemeBackgroundPipe();
		expect(pipe).toBeTruthy();
	});
});
